List<dynamic>? filterJasonBy(
  List<dynamic> json,
  String? key,
  String? value,
) {
  /// MODIFY CODE ONLY BELOW THIS LINE

  return json.where((item) => item[key] == value).toList();

  /// MODIFY CODE ONLY ABOVE THIS LINE
}
